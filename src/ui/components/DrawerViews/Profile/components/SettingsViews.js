import React from 'react';

import {
    View,
    Text,
    StyleSheet
} from 'react-native';

const accountSettings = (props) => {
    <View style={styles.container}>

    </View>
}

const notificationSettings = (props) => {
    <View style={styles.container}>
        
    </View>
}

const paymentSettings = (props) => {
    <View style={styles.container}>
        
    </View>
}

const privacySettings = (props) => {
    <View style={styles.container}>
        
    </View>
}

const styles = StyleSheet.create({
    container: {
        flex: 1
    }
})

export { accountSettings, notificationSettings, paymentSettings, privacySettings }
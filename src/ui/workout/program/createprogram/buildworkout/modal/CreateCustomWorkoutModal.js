import React from 'react';

import {
    View,
    Text,
    StyleSheet,
    Modal
} from 'react-native';

function CreateCustomWorkoutModal({ isVisible }) {
    return (
        <Modal visible={false} presentationStyle="fullScreen" animated={true} animationType="slide">
            
        </Modal>
    )
}

const style = StyleSheet.create({
    root: {

    }
})

export default CreateCustomWorkoutModal;
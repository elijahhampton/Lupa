import { combineReducers } from 'redux';

import actionTypes from './actionTypes';

const searchReducerInitialState = {
    
}

export const searchReducer = (state = searchReducerInitialState, action) => state

export default lupaRootReducer = combineReducers({search: searchReducer})
{
  search: {
    
  }
}